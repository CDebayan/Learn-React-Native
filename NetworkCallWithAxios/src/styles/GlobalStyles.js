import { StyleSheet } from 'react-native';

export const GlobalStyles = StyleSheet.create({
    screen: {
        flex: 1
    },
    centerContainer: {
        alignItems: 'center',
        justifyContent: 'center',
        flex: 1,
    },
});
